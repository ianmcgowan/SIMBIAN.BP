DONE = 0
LOOP
READNEXT @ID ELSE DONE = 1
UNTIL DONE DO
GOSUB PROCESS.RECS
REPEAT
CLEARSELECT
PROCESS.RECS:
*print records for clients in Paris."
READ REC FROM CLIENT.FILE,@ID
THEN PRINT REC
RETURN
END
